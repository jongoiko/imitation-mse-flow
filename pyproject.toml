[project]
name = "imitation-bc-fm"
version = "0.1.0"
description = "Behavior cloning experiments with MSE and flow matching"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.1",
    "einops>=0.8.2",
    "gym-pusht",
    "gymnasium>=1.3.0",
    "h5py>=3.16.0",
    "imageio>=2.37.4",
    "imageio-ffmpeg>=0.6.0",
    "pillow>=12.3.0",
    "robomimic>=0.3.0",
    "torch>=2.14.0",
    "tyro>=1.0.16",
    "wandb>=0.29.0",
    "zarr>=3.3.0",
]

[tool.uv.sources]
gym-pusht = { git = "https://github.com/jongoiko/gym-pusht.git" }

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
